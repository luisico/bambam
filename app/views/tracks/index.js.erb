<% if @tracks.count > 0 %>
  $('#tracks-by-projects').html('<%= j render(partial: "tracks/tracks_by_projects", locals: {tracks: @tracks}) %>');
<% else %>
  $('#tracks-by-projects').html('<li>No matches. <%= link_to 'Clear filter.', tracks_path, remote: true %></li>');
<% end %>
