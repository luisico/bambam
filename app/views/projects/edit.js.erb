$('#project-users').hide().after('<%= j render("users_form") %>');
$("#project_user_ids").select2({
  placeholder: "Add a user"
});

if (hopscotch.getState() === "tour-project-show-manager:6") {
  hopscotch.startTour(eval($('div[data-tour]').data('tour')), 6);
}
