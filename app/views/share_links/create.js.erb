<% if @share_link.errors.any? %>
  $('.new_share_link').remove();
  $('.track-share-links').append('<%= j render("form") %>');
  $('#share_link_expires_at').datepicker({ dateFormat: "'Expires on' d, MM yy" })
<% else %>
  $('.new_share_link').remove();
  $('#new_link').show();
  $('#share-links').html("<%= escape_javascript(render(:partial => @share_link.track.share_links)) %>");
<% end %>
